apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nana_webapp.fullname" . }}-test-connection"
  labels:
    {{- include "nana_webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nana_webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
