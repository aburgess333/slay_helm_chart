apiVersion: v1
kind: Service
metadata:
  name: {{ include "nana_webapp.fullname" . }}
  labels:
    {{- include "nana_webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nana_webapp.selectorLabels" . | nindent 4 }}
